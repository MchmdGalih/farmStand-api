<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Details Farms</title>
  </head>
  <body>
    <h1><%= farm.name %></h1>
    <ul>
      <li>City: <%= farm.city %></li>
      <li>Email: <%= farm.email %></li>
    </ul>

    <h1>Products</h1>
    <ul>
      <% for (let product of farm.products) {%>
      <li><a href="/products/<%= product._id %>"><%= product.name %></a></li>
      <% } %>
    </ul>

    <a href="/farms">Back to all Farms</a>
    <a href="/farms/<%= farm._id %>/products/new" %>">Add Products to Farms</a>

    <form action="/farms/<%= farm._id %>/delete?_method=DELETE" method="post">
      <button>Delete</button>
    </form>
  </body>
</html>
